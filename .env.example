# Cloudflare operator credentials used by setup.sh / deploy.sh.
# Copy to .env only when using API-token authentication instead of `wrangler login`.
# Never commit the real .env file.

CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
